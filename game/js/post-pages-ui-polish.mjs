// Compatibility shim retained temporarily while presentation-bootstrap removal is verified.
// All accepted screen-specific CSS now lives in owner stylesheets.
globalThis.RPChessPostPagesUIPolish = Object.freeze({ refresh() {} });
