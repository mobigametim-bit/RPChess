# Materialized game source

This directory is the exact build output produced from the former packaged source and deployment patches. It is committed as the Stage 1 behavioral baseline. Future work migrates these files into normal source modules and removes the ZIP/patch pipeline.
