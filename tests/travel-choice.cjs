const path=require('path'),assert=require('assert'),{pathToFileURL}=require('url');function memoryStorage(){const d=new Map();return{getItem:k=>d.has(k)?d.get(k):null,setItem:(k,v)=>d.set(k,String(v)),removeItem:k=>d.delete(k)}}
(async()=>{const game=path.resolve(__dirname,'..','game'),travel=await import(pathToFileURL(path.join(game,'js/travel-choice-core.mjs')).href),persistence=await import(pathToFileURL(path.join(game,'js/run-persistence.mjs')).href),skirmish=await import(pathToFileURL(path.join(game,'js/skirmish-core.mjs')).href),battle=await import(pathToFileURL(path.join(game,'js/battle-core.mjs')).href);assert.deepStrictEqual(travel.PLAYABLE_TRAVEL_TYPES,['skirmish','battle','settlement','event']);assert.strictEqual(travel.TRAVEL_CHOICE_COUNT,3);for(const type of travel.TRAVEL_ENCOUNTER_TYPES){assert(Array.isArray(travel.FLAVOR_POOLS[type]));assert(travel.FLAVOR_POOLS[type].length>=10);}
const first=travel.createTravelChoices({runId:'travel-test-run',step:1}),repeat=travel.createTravelChoices({runId:'travel-test-run',step:1});assert.deepStrictEqual(first,repeat);assert.strictEqual(first.length,3);assert.strictEqual(new Set(first.map(c=>c.id)).size,3);assert(first.every(travel.isTravelChoice));const duplicateFound=Array.from({length:100},(_,i)=>travel.createTravelChoices({runId:`dupe-${i}`,step:1})).some(f=>new Set(f.map(c=>c.type)).size<3);assert(duplicateFound,'independent pool must allow duplicate types in one fork');const counts={skirmish:0,battle:0,settlement:0,event:0};for(let i=0;i<4000;i++)for(const c of travel.createTravelChoices({runId:`distribution-${i}`,step:1}))counts[c.type]++;for(const [type,count] of Object.entries(counts)){const ratio=count/12000;assert(ratio>.22&&ratio<.28,`${type} ratio ${ratio} must stay near 25%`);}const run=persistence.createRun({id:'travel-run',now:1000}),storage=memoryStorage(),offered=persistence.writeRun({...run,currentTravelChoices:first},storage,1100);assert.deepStrictEqual(persistence.readRun(storage).currentTravelChoices,first);
const manualSkirmish={id:'manual.s',step:1,type:'skirmish',label:'СТЫЧКА',stars:2,threatLabel:'УМЕРЕННАЯ',flavor:'Путь.',mechanicalHint:'Нестандартный состав противника.',seed:'manual-s'};globalThis.RPChessTravelEncounterOverride=manualSkirmish;const rs=skirmish.createEncounter({seed:'fallback',stars:5});assert.strictEqual(rs.seed,'manual-s');assert.strictEqual(rs.stars,2);const manualBattle={...manualSkirmish,id:'manual.b',type:'battle',label:'БИТВА',seed:'manual-b'};globalThis.RPChessTravelEncounterOverride=manualBattle;const rb=battle.createBattleEncounter({seed:'fallback',stars:1});assert.strictEqual(rb.seed,'manual-b');assert.strictEqual(rb.stars,2);console.log('Travel Choice independent 25% pool, duplicate allowance, deterministic persistence and combat routing: PASS');})().catch(e=>{console.error(e.stack||e);process.exitCode=1});
