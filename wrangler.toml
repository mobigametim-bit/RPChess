name = "rpchess"
compatibility_date = "2026-08-04"

[build]
command = "npm run build && (command -v chromium || command -v chromium-browser || command -v google-chrome || command -v google-chrome-stable)"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
