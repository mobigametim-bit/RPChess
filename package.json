{
  "name": "rpchess",
  "private": true,
  "version": "2.0.0-foundation.1",
  "description": "RPChess Reboot — fantasy roguelite built on classical chess",
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "verify": "node -e \"require('./scripts/verify-source.cjs')(require('path').resolve('game'))\"",
    "test": "node tests/reboot-foundation.cjs",
    "test:foundation": "node tests/reboot-foundation.cjs",
    "build": "node scripts/build.cjs",
    "deploy": "wrangler deploy"
  }
}
